# SmartCart Segmentation System

An Unsupervised Machine Learning project that uses K-Means Clustering to segment customers into distinct groups based on purchasing behavior, enabling targeted marketing strategies.